<%- include('partials/_header') %>
    <title>moneymoo</title>
</head>
<body>
  <% if (user) { %>
    <%- include('partials/_navbar') %>
    <h1 class="is-size-2">Welcome back, <%= user.username %></h1>
  <% } else { %>
    <div class="container is-max-tablet">
      <h1 class="is-size-2 has-text-centered">Welcome to moneymoo</h1>
      <p class="is-size-5 has-text-centered">let's start milking your gains</p>
      <div class="has-text-centered">
        <a href="/auth/sign-up" class="button is-link">Sign Up</a>
        <a href="/auth/sign-in" class="button is-link">Log In</a>
      </div>
    </div>
  <% } %>
</body>
</html>